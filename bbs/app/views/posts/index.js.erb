$('#items').append("<%= escape_javascript(render 'items', object: @posts) %>")
$("#more_link").replaceWith("<%= escape_javascript(
  link_to_next_page(@posts, 'more', remote: true, id: 'more_link')
) %>");
